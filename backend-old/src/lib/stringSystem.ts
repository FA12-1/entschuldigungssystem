import { Core } from "./core";
//import { languages } from "../language/importer";

export class StringSystem {
    core: Core;
    
    constructor(core: Core) {
        this.core = core;
        //languages.de_DE["error"]["noData"]
    }

    getLanguage(lang: string) {
        
    }
}